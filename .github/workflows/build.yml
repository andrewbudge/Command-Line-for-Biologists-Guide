name: Build Book PDF

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Compile LaTeX
        uses: xu-cheng/latex-action@v3
        with:
          working_directory: book
          root_file: main.tex
          args: -pdf -interaction=nonstopmode -halt-on-error -file-line-error

      - name: Rename PDF for release
        run: mv book/main.pdf book/Command-Line-for-Biologists.pdf

      - name: Update "latest" release with new PDF
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Latest build
          files: book/Command-Line-for-Biologists.pdf
          body: |
            Auto-built from commit ${{ github.sha }}.

            This is a work-in-progress — content changes frequently.
            Download the PDF below to see the current state of the book.
